const initialState ={
    tsxt: '""',
   id: 0,
   categoryId: null,

}

export default function toDoList(state = initialState, action){
    switch(action.type){
        
        default:
            return state
    }
}